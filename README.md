# Money Control

Money Control is a local-first personal expense tracking application built with Expo, React Native, TypeScript, and Expo Router.

## Development

```bash
npm run start
```

Available checks:

```bash
npx tsc --noEmit
npm run lint
```
