import { useCallback, useState } from 'react'; import { accountService } from '@/features/accounts/accounts'; import { bogotaToday, monthFromDate } from '@/features/transactions/transaction-date'; import { transactionService } from '@/features/transactions/transactions'; import type { MonthlyTransactionSummary, TransactionListItem } from '@/features/transactions/transaction.types'; import { useFinancialDataRefresh } from '@/hooks/use-financial-data-refresh';
type State = { totalBalance: number; summary: MonthlyTransactionSummary; recent: TransactionListItem[] };
export function useHomeDashboard() { const [data, setData] = useState<State>({ totalBalance: 0, summary: { income: 0, expenses: 0, net: 0 }, recent: [] }); const [loading, setLoading] = useState(true); const [error, setError] = useState<string>(); const month = monthFromDate(bogotaToday()); const reload = useCallback(async () => { setLoading(true); setError(undefined); try { const [accounts, summary, recent] = await Promise.all([accountService.list(true), transactionService.summarizeMonth(month), transactionService.recent(3)]); setData({ totalBalance: accountService.calculateNetWorth(accounts), summary, recent }); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to load dashboard.'); } finally { setLoading(false); } }, [month]); useFinancialDataRefresh(reload); return { ...data, month, loading, error }; }
